<nav class="menu">
  <div class="menu-container">
    <div class="logo">
      <span class="logo-icon">☕</span>
      <span class="logo-text">Cafetería Aroma</span>
    </div>
    <div class="menu-links">
      <a href="/" class="menu-link">Inicio</a>
      <a href="/productos" class="menu-link">Productos</a>
      <a href="/contacto" class="menu-link">Contacto</a>
    </div>
  </div>
</nav>

<style>
.menu {
  background: linear-gradient(135deg, #4a3428 0%, #6f4e37 100%);
  padding: 0;
  box-shadow: 0 5px 20px rgba(0, 0, 0, 0.15);
  position: sticky;
  top: 0;
  z-index: 1000;
  backdrop-filter: blur(10px);
}

.menu-container {
  max-width: 1400px;
  margin: 0 auto;
  padding: 1.2rem 2rem;
  display: flex;
  justify-content: space-between;
  align-items: center;
}

.logo {
  display: flex;
  align-items: center;
  gap: 0.8rem;
  font-family: 'Playfair Display', serif;
  font-size: 1.6rem;
  font-weight: 700;
  color: white;
  text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.3);
}

.logo-icon {
  font-size: 2rem;
  animation: rotate 3s ease-in-out infinite;
}

@keyframes rotate {
  0%, 100% {
    transform: rotate(0deg);
  }
  50% {
    transform: rotate(15deg);
  }
}

.menu-links {
  display: flex;
  gap: 2.5rem;
  align-items: center;
}

.menu a,
.menu a:visited,
.menu a:link,
.menu-link {
  color: #ffffff !important;
  text-decoration: none;
  font-weight: 600;
  font-size: 1.1rem;
  padding: 0.6rem 1.2rem;
  border-radius: 8px;
  transition: all 0.3s ease;
  position: relative;
  letter-spacing: 0.5px;
  display: inline-block;
  -webkit-text-fill-color: #ffffff !important;
}

.menu-link::after {
  content: '';
  position: absolute;
  bottom: 0;
  left: 50%;
  transform: translateX(-50%);
  width: 0;
  height: 3px;
  background: linear-gradient(90deg, #c4a572, #d4a574);
  transition: width 0.3s ease;
  border-radius: 2px;
}

.menu a:hover,
.menu-link:hover {
  background: rgba(255, 255, 255, 0.1);
  transform: translateY(-2px);
  color: #ffffff !important;
  -webkit-text-fill-color: #ffffff !important;
}

.menu-link:hover::after {
  width: 80%;
}

@media (max-width: 768px) {
  .menu-container {
    flex-direction: column;
    gap: 1rem;
  }
  
  .menu-links {
    gap: 1.5rem;
  }
  
  .logo {
    font-size: 1.3rem;
  }
  
  .menu-link {
    font-size: 1rem;
    padding: 0.5rem 1rem;
  }
}
</style>
